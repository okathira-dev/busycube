# Project-scoped Codex configuration. Keep credentials and personal MCP servers out of this repository.

[mcp_servers.serena]
startup_timeout_sec = 15
command = "serena"
args = ["start-mcp-server", "--project-from-cwd", "--context=codex"]
